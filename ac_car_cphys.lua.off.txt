
local _silv, _sirn

---Returns list of setup items. If you need to access the value of a certain item, consider using `ac.getSetupItem(index)` instead,
---it would run a lot faster.
---
---Note: setup items from custom physics are only available for player’s car at the moment.
---@return {id: string, value: number, multiplier: number}[]
function ac.getSetupItems()
  if not _sirn then _sirn = refnumber() end
  ffi.C.lj_getSetupItems_inner__cphys(_ssrn)
  _silv = __util.result() or _silv
  return _silv or error('Failed to get data', 2)
end

